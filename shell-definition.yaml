tosca_definitions_version: tosca_simple_yaml_1_0

metadata:
  template_name: Terraform Service 2G
  template_author: Quali CS
  template_version: 1.0.0
  template_icon: shell-icon.png

description: >
  TOSCA based service shell

imports:
  - cloudshell_standard: cloudshell_custom_service_standard_1_0_0.yaml

node_types:

  vendor.resource.Terraform Service 2G:
    derived_from: cloudshell.nodes.CustomService
    properties:
    #  User:
    #    description: User with administrative privileges
    #    type: string
    #    tags: [user_input]
    #  Password:
    #    type: cloudshell.datatypes.Password
    #    tags: [user_input]
    #  Address:
    #    description: Address for remote access
    #    type: string
    #    tags: [user_input]
    #  Service Categories:
    #    type: list
    #    default: [My Category 1, My Category 2]
      Terraform Module Path:
        description: path to target module. Target directory will be changed to here before running terraform commands.
        type: string
        tags: [ user_input ]
      Terraform Executable:
        description: path to folder with terraform.exe
        type: string
        tags: [ user_input ]
    artifacts:
      icon:
        file: shell-icon.png
        type: tosca.artifacts.File
      driver:
        file: TerraformService2GDriver.zip
        type: tosca.artifacts.File
